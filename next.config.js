/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // If you're using the App Router, no extra config needed.
  // Add any redirects/rewrites/etc here later.
};

module.exports = nextConfig;

